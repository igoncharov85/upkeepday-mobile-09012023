import { StyleSheet } from "react-native/types";

export default StyleSheet.create({
    container: {
        width: '100%',
        height: 52,
        borderRadius: 8,
    },
    labelText: {
        fontSize: 12,
        fontWeight: '400'
    },
    absoluteIcon: {
        position: 'absolute',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center',
        left: 20,
    }
})